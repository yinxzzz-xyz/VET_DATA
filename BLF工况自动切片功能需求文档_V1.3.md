# BLF 工况自动切片功能需求文档

## 文档信息

|项目|内容|
|-|-|
|版本|V1.3|
|状态|已确认功能需求，已按现有源码和实际 CSV 补充实现约束|
|范围|BLF 工况切片功能及必要交互|

## 1\. 目的与范围

本文档定义 BLF 工况自动切片的输入、用户流程、时间规则、处理逻辑、输出、异常处理、性能与验收标准，并依据现有 VET\_DATA Python 源码和实际 CSV 样例约束软件架构、GUI 接入、代码目录、线程模型、依赖、接口、配置、打包和测试方式。

## 2\. 背景与目标

CANoe 采集的 CAN/CAN FD 数据可能分成多个时长不同的 BLF。文件名可能含开始时间，但没有可靠结束时间；文件间可能连续、重叠或存在断点。系统应根据 CSV/XLSX 中记录的工况时间，提取工况前后指定范围内的报文，生成可追溯的独立 BLF 和处理报告。

文件候选匹配以 BLF Header 起止时间为主要依据，文件名时间只作辅助；最终筛选以报文 timestamp 为准。系统应支持连续分卷的跨文件切片，避免多个工况重复扫描同一源文件。

## 3\. 支持范围

* 输入 CANoe 生成的 CAN/CAN FD BLF。
* 输入 `.csv` 或 `.xlsx` 工况表。
* 输出只保留普通 CAN/CAN FD 数据帧；Remote Frame/RTR（`is\_remote\_frame=True`）、错误帧（`is\_error\_frame=True`）、环境变量、日志标记及其他总线对象均不写入，并在报告中作为忽略对象统计。
* 保留 CAN ID、数据、DLC、标准/扩展帧标志、CAN FD/BRS/ESI 标志、通道及 timestamp，确保可继续使用 DBC 或 CAN 类型 ARXML 解码信号。

## 4\. 输入与默认值

|输入项|规则|
|-|-|
|BLF|选择一个文件或一个文件夹|
|工况表|一个 CSV 或 XLSX|
|输出目录|可选|
|向前/向后时间|每条工况独立设置；默认各 60 秒|
|BLF/表格时区|分别设置、整个任务共用；默认 UTC+08:00|

选择文件夹时只读取其直接包含的 BLF，不递归子文件夹，也不支持同时选择多个文件夹。前后时间均为 0～300 的整数秒，允许一项为 0，不允许同时为 0。

未选择输出目录时：单个 BLF 使用其所在目录；文件夹输入使用所选文件夹。UI 显示实际路径并提醒用户未单独选择输出目录。默认位置不可写时必须另选。用户选择的输出目录必须存在且可写，允许与输入目录相同，不创建额外任务或日期子目录。

## 5\. 工况表规则

第一行为固定表头，第二行开始为数据：

|表头|含义|
|-|-|
|记录时间|工况时间|
|记录内容|工况名称|
|向前秒数（可选，允许表头没有这一项）|工况时间 - 向前秒数|
|向后秒数（可选，允许表头没有这一项）|工况时间 + 向后秒数|



不提供列映射；XLSX 只读第一个工作表。支持 Excel 原生日期时间、具有合法缓存结果的公式，以及文本格式 `yyyy-MM-dd HH:mm:ss` 和实际 CSV 使用的 `yyyy/M/d H:mm`；分钟精度的时间将秒补为 `00`。CSV 按文本解析。不支持毫秒、日期时间分列或只有时间没有日期。年份限 2000～2100，不将普通数字猜测为日期。

空白、无有效公式缓存、无法解析或超出范围的行直接舍弃，向用户说明文件/工作表和行号，不影响其他有效行。没有有效工况时禁止继续。

实际 CSV 样例为带 UTF-8 BOM 的 UTF-8 编码、英文逗号分隔，并使用符合 CSV 规则的双引号包裹含逗号、引号或换行的字段。实现必须使用标准 CSV 解析能力处理引号转义和多行“记录内容”，不得按物理行或直接按逗号拆分。CSV 首选 `utf-8-sig` 解码；若解码失败，可按现有软件兼容策略回退 GBK，并在确认窗口和报告中记录实际采用的编码。除固定必需列外，样例中的“序号”“时间来源”“纬度”“经度”等附加列允许存在并忽略。

## 6\. 时区与时间窗

V1.3 使用固定 UTC 偏移，不处理夏令时。第一级提供 UTC-12:00 至 UTC+14:00 的整小时选项，并提供“其他”，以 15 分钟为最小粒度选择非整点偏移。默认 UTC+08:00。

BLF 与表格时间必须转换到统一内部基准。实现时须验证解析库的时间语义，避免重复施加偏移。

```text
开始时间 = 工况时间 - 向前秒数
结束时间 = 工况时间 + 向后秒数
```

时间窗为闭区间，timestamp 等于开始或结束时间的报文均包含。

## 7\. 工况确认窗口

选择 BLF 和表格后，系统解析表格并打开确认窗口，展示选择状态、工况名称、工况时间、每条工况的前后秒数、任务级两个时区和实际输出目录。所有有效工况默认全选，前后各 60 秒，时区均为 UTC+08:00。

用户可以：

* 修改工况名称、工况时间及每条工况的前后秒数；修改不回写源表格；
* 修改默认的工况前后秒数；
* 修改任务级 BLF 和表格时区；
* 逐项选择、多选、全选或全部取消；
* 改选输出目录；
* 点击“开始切片”；
* 在切片开始前关闭窗口以取消本次任务，此时不生成输出或报告。

点击开始时统一校验所有已选工况。至少选择一项；名称、时间、秒数、时区和输出目录必须有效。若未选择任何工况，直接提示且不开始。任一已选行无效时不开始，并定位错误；未选行的无效内容不阻止任务。

## 8\. 索引和文件匹配

系统读取每个 BLF Header 建立路径、开始时间、结束时间和读取状态索引；在锁定的 `python-can 4.6.1` 中，正常 Header 索引必须使用 `can.BLFReader.start\_timestamp` 和 `can.BLFReader.stop\_timestamp`。创建 reader 后，正常索引不得迭代报文，也不得按固定文件时长推算。

Header 缺失、非法、起止颠倒或不可信时，允许降级扫描报文，以第一条和最后一条有效 CAN/CAN FD 数据帧 timestamp 恢复范围。不可信条件至少包括：`start\_timestamp <= 0`、`stop\_timestamp <= 0`、`stop\_timestamp < start\_timestamp`、转换后的年份不在 2000～2100，以及 reader 构造或 Header 解析异常。降级扫描必须显示进度、支持取消并记录触发原因；无法恢复则标记损坏并继续其他文件。Header 用于快速候选匹配，最终覆盖范围以完整读取后的实际报文时间为准，不修改源 Header。

系统为每个工况查找所有与闭区间时间窗重叠的文件。与任何已选工况无关的 BLF 不完整读取。

## 9\. 分卷、重叠与重复文件

* 所有可用 BLF 先按 Header 开始时间、规范化完整路径稳定排序；降级扫描恢复时间范围的文件以恢复后的开始时间参与排序。
* 按排序结果从前向后建立来源链。同一来源链仅允许下一文件与该链最后一个文件不重叠且间隔不大于 5 秒；满足条件的连续 CANoe 分卷可合成一个跨文件工况切片。
* 某文件可加入多条已有来源链时，优先加入满足条件且链尾结束时间与该文件开始时间最接近的链；距离仍相同则按链内首个文件的规范化完整路径稳定决定。无可加入的链时新建来源链。
* 任意两个时间范围发生重叠的文件不得进入同一来源链，视为不同录制来源并分别生成切片。路径排序只用于输入和歧义决策稳定化，不代表重叠文件可以合并。
* 相邻文件 Header 首尾间隔大于 5 秒，判定为文件间断点。
* 不自动校正不同录制设备的时间，使用各自原始 timestamp。
* 重复文件检测采用两阶段性能策略：先按文件大小分组，大小唯一的文件不计算完整哈希；对大小相同的候选文件可先计算分块头尾快速摘要；仅对摘要仍相同的疑似项，按块流式计算完整 SHA-256。只有文件大小和完整 SHA-256 均一致才判定为完全重复。摘要与哈希阶段均须显示进度并支持取消。
* 每个完全重复组在确认界面中只能单选保留一个文件。用户确认所有重复组后才能继续；允许在该界面取消整个任务。未确认、选择缺失或同组选中多个文件时不得开始切片。
* 时间重叠、CAN ID 相同或部分报文相同不足以证明完全重复，不自动去重。

## 10\. 扫描与报文筛选

先建立“源 BLF—相关工况窗”关系，再按源文件处理。每个相关 BLF 应尽量只完整扫描一次，并在一次扫描中把报文分配给全部命中的工况；同一报文可进入多个工况输出。多工况匹配应使用时间索引或排序，避免逐报文无条件遍历全部工况。

仅写入时间窗内普通 CAN/CAN FD 数据帧；Remote Frame/RTR、错误帧和其他对象均跳过并分类统计。连续且不重叠的分卷按实际起始时间排序后依次流式读取和写入，不制造数据；不得对单文件全部报文或跨文件全部报文执行内存全局排序。默认保持单个 CANoe BLF 内的源记录顺序；检测到单文件 timestamp 轻微倒序时记录警告并保持源顺序。timestamp 无法解析或明显异常的单条报文跳过并报告。重叠来源分别处理和输出，不跨来源排序或去重。

## 11\. 状态与断点

* **完整**：可靠 BLF 的有效时间范围连续覆盖整个目标时间窗，所有相关文件均正常读取，且目标窗内最终存在至少一条普通 CAN/CAN FD 数据帧，处理成功。
* **部分缺失**：目标窗端部或中间存在文件级覆盖缺口，或任一相关文件损坏、读取失败；目标窗内最终仍存在至少一条普通 CAN/CAN FD 数据帧。
* **完全无数据**：目标窗内最终没有任何普通 CAN/CAN FD 数据帧；不生成空 BLF。即使 Header 范围覆盖目标窗，只要筛选后目标报文数为 0，仍判为完全无数据。
* **失败**：解析、读取或写入错误导致未完成。
* **取消**：处理中被用户取消。
* **未处理**：取消时尚未开始的已选工况。
* **未选择**：用户未选择处理。

覆盖状态以相关源 BLF 的可靠有效时间范围及文件可读状态判断，不以 CAN 报文密度判断。可靠时间范围连续覆盖整个目标窗且相关文件均正常读取时，文件级覆盖条件满足；窗口端部或中间存在文件级覆盖缺口，或相关文件损坏、读取失败时，文件级覆盖条件不满足。然后以目标窗内最终筛选出的普通 CAN/CAN FD 数据帧数量作最高优先级判定：数量为 0 时一律为“完全无数据”；数量大于 0 时，再根据文件级覆盖条件判为“完整”或“部分缺失”。读取或写入错误导致无法形成可信结果时按“失败”处理，不得用前三种覆盖状态掩盖错误。

单个正常 BLF 内所有 CAN/CAN FD 数据帧之间超过 5 秒无报文，只提示“检测到长时间无报文区间”，不改变覆盖状态，也不按通道判断断点。用户不能修改断点阈值。不可读数据不得解释为正常无数据。

## 12\. 输出与命名

连续且不重叠的分卷可生成一个 BLF；时间重叠的不同来源分别输出。基本文件名为：

```text
工况名称-yyyyMMdd-HHmmss.blf
```

删除 `< > : " / \\\\\\\\\\\\\\\\ | ? \\\\\\\*`、首尾空格和首尾句点。Windows 保留名称、清洗后为空或过长时阻止开始并要求修改。同名同时间工况、重叠来源或输出目录已有同名文件时，依次添加 `-1`、`-2` 等后缀，不覆盖原文件。重叠来源编号按源 BLF 规范化完整路径排序。报告保留原始名称、修改后名称与实际文件名的对应关系。

输出 BLF 文件名（含 `.blf` 扩展名及可能的递增后缀）最长 180 个字符。开始处理前必须基于每个最终候选文件名及实际输出目录校验完整输出路径符合 Windows 安全路径限制；文件名或完整路径超限时不得开始，并要求用户修改工况名称或输出目录。不得依赖运行到写入阶段后再发现路径超限。

每个输出先写入与正式文件同目录的临时文件，临时文件名固定为“目标文件名.blf.part”，即在已经包含 `.blf` 的正式文件名后追加 `.part`。写入完整、关闭 writer 并完成必要校验后，才以不覆盖现有文件的原子重命名提交为正式 `.blf`；提交前若发生同名竞争，重新分配冲突编号并相应调整临时文件名。

## 13\. 结果窗口和报告

完成、部分完成或处理开始后取消时，显示结果窗口，并在实际输出目录生成 Markdown 报告。报告基本文件名固定为 `BLF切片报告-yyyyMMdd-HHmmss.md`；若已存在，则依次使用 `BLF切片报告-yyyyMMdd-HHmmss-1.md`、`BLF切片报告-yyyyMMdd-HHmmss-2.md` 等，不覆盖原文件。

报告至少包含任务起止时间、状态、输入与输出路径、两个时区、索引耗时、BLF 有效/异常数量、表格有效/舍弃行和原因、各状态数量，以及逐工况的原始行号、名称、时间、前后秒数、目标窗、相关 BLF、实际首末报文时间、报文数、输出文件、状态、断点、警告和错误。还应分类记录被忽略的 Remote Frame/RTR、错误帧和其他对象，记录重复检测阶段、每组保留选择、文件名/路径长度校验、文件名冲突及临时文件提交结果。不要求分别统计 CAN/CAN FD 数量、通道或报文 ID。

## 14\. 进度与取消

提供输入检查、表格解析、重复检测、Header 索引、降级扫描、当前文件、文件计数、输出和报告进度。总耗时不设上限，用户必须知道任务仍在进行且可取消。

点击取消后立即显示“正在取消”。正常情况下 3 秒内进入安全收尾；底层单次读取不可中断时可等待其返回。停止安排新处理，保留已完整输出；当前工况标为取消并删除临时文件；未开始的已选工况标为未处理；任务总状态为已取消，并生成结果窗口和报告。任何情况下不得修改、移动或删除原始 BLF 或源表格。

## 15\. 异常处理

* 路径不存在、不可读、输出不可写、无 BLF、缺少固定表头或无有效工况：禁止开始并说明原因。
* 空 BLF 或无 CAN/CAN FD：记录并继续。
* 部分 BLF 损坏：继续处理其他文件，任务为部分完成，受影响工况不得标为完整。
* 工况字段无效：确认窗口定位并禁止开始。
* 写入失败或磁盘不足：关闭输出、删除未完成临时文件并记录失败。
* 表格异常应定位到原始行；无效行跳过，不影响其他有效行。

## 16\. 性能要求

* 正常索引只读 Header；只完整读取相关 BLF；相关 BLF 尽量只扫描一次。
* 采用流式读写，不一次载入整个 BLF。
* 正常峰值内存不超过 1 GB，推荐不超过 512 MB，按最低 8 GB 系统内存设计。
* 典型 5～20 个 BLF，最大 50 个；单文件最大 1 GB，总量最大 20 GB；工况最多 100 条。
* 本地正常 BLF：5～20 个 Header 索引建议 5 秒内完成，50 个建议 10 秒内完成。
* 上述为软目标而非超时限制。超出后继续、显示进度并允许取消，不得仅因超时判定失败；异常文件的降级扫描不计入此目标。

## 17\. 验收标准

1. CSV/XLSX 表头、时间、公式缓存、无效行和中文内容按规则解析。
2. 确认窗口默认全选、前后各 60 秒、时区 UTC+08:00，并支持全部编辑和选择操作。
3. 未选工况或任一已选行非法时不开始并准确提示。
4. 时区转换正确，闭区间两端报文均保留。
5. Header 索引不全量扫描；异常 Header 可降级恢复。
6. 连续分卷正确合成；文件间超过 5 秒正确标记断点。
7. 重叠来源分别输出；完全重复文件要求用户择一。
8. 同一 BLF 服务多个工况时尽量仅完整扫描一次。
9. 输出仅含窗内 CAN/CAN FD，且可继续用 DBC/CAN ARXML 解码。
10. 覆盖状态按可靠文件时间范围和可读性确定，报文密度只触发警告；目标窗最终无目标数据帧时判为完全无数据。完整、部分缺失、失败、取消、未处理和未选择状态均符合确定性规则。
11. 命名非法字符、180 字符文件名上限、Windows 完整路径限制、重复及已有文件均不造成覆盖且可追溯。
12. 默认/指定输出位置正确；完全无数据不生成空 BLF。
13. 取消保留完整结果、删除临时文件，绝不影响源文件。
14. 结果窗口、Markdown 报告与实际输出的时间、数量、来源和状态一致。
15. 规定数据规模下采用流式处理，并满足内存上限与软性索引目标；不进行单文件或跨文件全量内存排序。
16. Remote Frame/RTR、错误帧及其他对象均不写入，并在报告中分类统计。
17. 正常 Header 索引使用 python-can 4.6.1 的起止属性且不迭代报文；全部不可信条件均能触发可取消的降级扫描。
18. 重叠来源分组算法在歧义输入下结果稳定，发生重叠的文件绝不进入同一来源链。
19. 重复检测按文件大小、快速摘要和完整 SHA-256 分阶段执行；每组必须且只能保留一个文件，未全部确认或取消时不得继续。
20. 报告使用固定基本命名并递增避让；输出通过固定 `.blf.part` 临时名写入，成功后原子提交，失败或取消不遗留未完成文件。

## 18\. 现有软件接入与实现约束

### 18.1 软件环境和现有架构

现有应用是 Windows 桌面 Python 程序，GUI 使用 PyQt6，主程序通过 `vet\_data/VET\_DATA\_merged.py` 启动，随后进入 `vet\_data\_modular.main.main()`。`main()` 负责 `multiprocessing.freeze\_support()`、控制台编码容错、pyqtgraph 抗锯齿和 Qt WebEngine 本地文件访问参数，再创建 `MDFPlotter` 主窗口。

`vet\_data\_modular.window.MDFPlotter` 通过多重继承在正式基线 `VET\_DATA\_v26.09.01.py` 的 `MDFPlotter` 上叠加信号列表、曲线交互、折叠面板、可搜索计算通道和后台文件加载能力。正式基线文件当前位于项目根目录；运行或打包前必须将其放到 `legacy.py` 所约定的 `vet\_data/VET\_DATA\_v26.09.01.py`，或等价地修改装载路径并验证。不得维护两份内容不同的同名基线。

当前验证环境为 Windows 64 位、Python 3.13.12，主要已安装包包括 PyQt6 6.11.0、PyQt6-WebEngine 6.11.0、python-can 4.6.1、pandas 3.0.1、numpy 2.4.2、asammdf 8.8.24、pyqtgraph 0.14.0、cantools 43.0.0 和 canmatrix 1.2。上述版本是当前可复现基线，不作为无条件升级要求；发布前应固化并验证依赖版本。XLSX 解析新增依赖 `openpyxl`，当前环境尚未安装，开发与打包时必须补充并锁定兼容版本。

### 18.2 GUI 布局和功能入口

主窗口保持现有左右布局：左侧为文件选择、信号搜索与选择、配置、计算通道、可用信号、CAN 通道配置、GPS 轨迹和底部操作区；右侧为信号曲线。新增“BLF 工况切片”按钮放在左侧底部操作区，与“绘制所选信号”“导出为CSV”“数据截取”同级，建议紧邻“数据截取”。

该入口是独立任务入口，不要求用户先通过“选择数据文件”加载单个文件，也不复用当前 `mdf\_path` 作为隐式输入。按钮不得被现有 `set\_buttons\_enabled(False)` 一并禁用；只在切片确认窗口或切片任务已经运行时临时禁止重复启动。入口点击后打开独立的非阻塞选择/确认流程，且不得清空当前已加载数据、已选信号、曲线、DBC/ARXML 配置或 GPS 状态。

确认窗口使用 `QDialog`，至少分为输入区、任务级设置区、工况表格区和操作区：输入区选择单个 BLF 或直接包含 BLF 的文件夹、工况表和输出目录；任务级设置区设置 BLF/表格时区；表格区展示选择框、原始行号、名称、时间、向前秒数、向后秒数和校验状态；操作区提供全选、全部取消、开始切片和取消。工况数量较多时表格必须可滚动，错误行应自动定位并选中首个无效单元格。

### 18.3 模块边界和接口

BLF 自动切片不得继续堆叠到 5000 行以上的基线文件中，应在 `vet\_data/vet\_data\_modular` 下新增独立模块，并仅在 `window.py` 中增加入口和生命周期接线。建议采用以下边界，实际文件名可调整，但职责不得回流到 GUI：

|模块|主要职责|建议接口|
|-|-|-|
|`blf\_slice\_models.py`|任务、工况、文件索引、输出结果和枚举状态的数据模型|`SliceTask`、`ConditionSpec`、`BlfIndexEntry`、`ConditionResult`、`TaskResult`|
|`blf\_slice\_table.py`|CSV/XLSX 解析、固定表头校验、原始行号和舍弃原因|`parse\_condition\_table(path) -> TableParseResult`|
|`blf\_slice\_service.py`|路径校验、Header 索引、降级扫描、哈希去重、来源分组、一次扫描多窗分配、流式写入和临时文件清理|`validate\_task(task)`、`build\_blf\_index(task, callbacks)`、`execute\_slice(task, callbacks, cancel\_token)`|
|`blf\_slice\_report.py`|Markdown 报告、文件名清洗、冲突编号和结果汇总|`build\_output\_name(...)`、`write\_report(task\_result, output\_dir) -> Path`|
|`blf\_slice\_dialog.py`|输入与工况确认窗口，不执行文件 I/O 密集工作|`BlfSliceDialog`|
|`blf\_slice\_progress.py`|进度窗口、结果窗口和后台线程适配|`BlfSliceWorker`、`BlfSliceProgressDialog`、`BlfSliceResultDialog`|

GUI 只传递不可变任务快照并接收信号，不直接遍历 BLF 或写文件。后台层不得创建或修改 QWidget。任务快照至少包含输入模式及路径、表格路径、实际输出目录、两个固定 UTC 偏移、已选和未选工况、每条工况的原始行号/原始名称/编辑后名称/时间/前后秒数，以及任务开始时间。

底层 BLF 读取和写入沿用现有 `python-can` 能力，使用 `can.BLFReader` 和 `can.BLFWriter`。写入时优先调用 `on\_message\_received(msg)`；若锁定版本确认提供 `write(msg)`，可封装兼容分支，但业务层不得散布版本判断。CAN/CAN FD 判断、通道和 FD 标志必须以实际 `python-can.Message` 属性为准；`is\_remote\_frame=True` 或 `is\_error\_frame=True` 的消息必须在统一筛选层排除并计数。禁止调用现有 `cut\_blf\_by\_time()` 直接实现批量切片，因为该方法面向单文件和单时间窗、会重复扫描，并且当前实现无法满足 Header 索引、跨文件合并、重叠来源隔离、重复文件确认、完整状态和任务报告要求。

### 18.4 线程、进度、取消和日志接入

沿用现有 `QThread + pyqtSignal` 模式，新建专用 `BlfSliceWorker`，不得复用按 Bus 解码的 `ParseWorker` 或普通数据加载的 `DataLoadWorker`。建议至少提供以下信号：

|信号|载荷|用途|
|-|-|-|
|`phase\_changed`|阶段、说明|输入检查、表格解析、重复检测、Header 索引、降级扫描、切片、输出、报告和安全收尾|
|`progress\_changed`|当前值、总值、当前文件|确定进度；总量未知时进度窗口切换为忙碌状态|
|`log\_emitted`|级别、消息、可选对象标识|结果窗口摘要和报告明细的统一来源|
|`duplicate\_found`|重复组信息|暂停任务并由 GUI 请求用户择一，后台线程不得弹窗|
|`finished`|`TaskResult`|正常完成或部分完成|
|`cancelled`|`TaskResult`|完成安全收尾后的取消结果|
|`failed`|错误摘要、`TaskResult`|任务级异常，同时保留已完成工况|

取消采用线程安全的 `threading.Event` 或等价令牌；在文件间、批次读取间、哈希计算块间、报文循环内和输出提交前检查。点击取消后，GUI 立即把按钮和状态改为“正在取消”，停止安排新文件或新工况；不得使用 `QThread.terminate()`。底层一次读取返回后删除当前 `.part` 临时文件，保留已通过原子重命名提交的完整 BLF，再生成取消报告。关闭进度窗口等同于请求取消；主窗口关闭时若任务仍运行，应复用现有后台加载的安全等待思路，先请求取消并在 worker 结束后再关闭。

运行日志使用 Python `logging`，业务模块不得依赖 `print`。日志经 `log\_emitted` 显示，并作为结构化事件同步进入最终 Markdown 报告；默认不额外生成独立日志文件。日志至少包含阶段、源文件、工况标识、警告/错误类型和异常摘要，但不得记录报文数据正文。未处理异常应保留 traceback 到调试日志，同时向用户显示可理解的摘要。

### 18.5 配置和持久化

现有程序用工作目录下的 `map\_config.json` 保存 GPS 选择，并允许用户另存/加载信号配置 JSON；BLF 自动切片不得改写或复用这两种配置。V1.3 不新增永久任务配置，前后 60 秒、UTC+08:00、300 秒上限、5 秒断点阈值、180 字符输出文件名上限和 Header 合法年份范围作为具名常量集中定义；用户在确认窗口的修改只存在于本次任务快照和报告中。

不得把输入路径、输出路径或最近任务写入源码目录。若后续决定记忆最近目录，应单独使用 `QSettings` 或用户配置目录，并作为新需求评审。临时输出与最终文件位于用户选择的输出目录，临时文件严格使用“正式 `.blf` 文件名后追加 `.part`”的命名；成功后以不覆盖已有文件的原子重命名提交。

### 18.6 依赖和打包

新增直接依赖仅限 `openpyxl`；CSV 使用 Python 标准库或 pandas 的标准 CSV 解析能力，BLF 继续使用现有 python-can。哈希、时间、路径、日志、线程同步和 Markdown 生成优先使用标准库。不得为固定 UTC 偏移引入完整时区数据库依赖。

项目当前没有 `requirements.txt`、`pyproject.toml`、PyInstaller `.spec` 或其他正式打包脚本，因此本功能交付时必须补齐可复现依赖清单和 Windows 打包配置。推荐沿用单一 GUI 可执行程序，入口为 `vet\_data/VET\_DATA\_merged.py`。打包验证必须覆盖 PyQt6-WebEngine 资源、pyqtgraph、asammdf、python-can 的 BLF 支持、cantools、canmatrix、pandas、numpy、openpyxl，以及通过 `importlib` 动态加载的正式基线文件。若保持当前动态加载方式，基线 `.py` 必须作为数据文件放在 `legacy.py` 预期位置；更稳妥的实现是将基线变为可正常导入的包模块后再打包。

打包后的程序不得要求控制台存在；用户可见错误通过对话框、进度窗口和报告呈现。需在无开发环境的 Windows 64 位机器上验证启动、中文路径、长路径、只读目录、包含空格的路径、WebEngine 资源和 BLF 读写。构建产物必须保留 `multiprocessing.freeze\_support()`。

### 18.7 回归测试

现有 `tests/test\_modular.py` 的窗口控件、信号搜索排序、计算通道和 CSV 加载测试必须继续通过。新增测试应与现有 `unittest` 体系兼容，并优先把 GUI 之外的业务逻辑作为纯函数或服务测试。

至少补充以下测试组：

1. 表格解析：UTF-8 BOM 逗号 CSV、GBK 回退、引号内逗号/换行、真实 `yyyy/M/d H:mm`、严格秒格式、中文、附加列、缺列、空白行、无效日期、年份边界、XLSX 首表、原生日期和公式缓存。
2. 时间与命名：两个固定偏移的换算、闭区间边界、前后为 0 的组合、非法字符、Windows 保留名、空名称、180 字符文件名上限、完整路径限制、报告固定命名、连续冲突编号、固定 `.blf.part` 临时名及原子提交。
3. 索引与匹配：python-can 4.6.1 正常 Header 属性读取且不遍历报文、各类不可信 Header 降级、损坏/空文件、无关文件不完整扫描、文件夹不递归、连续分卷、5 秒边界、超过 5 秒断点，以及按稳定排序、最近链尾和路径决策的重叠来源分组。
4. 扫描与输出：同一源文件服务多个工况时只扫描一次、同一报文进入多个窗口、CAN/CAN FD 属性保留、Remote Frame/RTR 和错误帧排除并统计、其他对象忽略、连续分卷依次流式读写、单文件轻微倒序警告且保持源序、禁止全量内存排序、完全无数据不生成 BLF、现有文件不覆盖和报告可追溯。
5. 状态与异常：完整、部分缺失、完全无数据、失败、取消、未处理和未选择；单文件内部长间隔仅警告；文件损坏、写入失败和磁盘不足后的临时文件清理。
6. 取消与线程：各主要阶段取消、重复文件哈希中取消、重复组每组单选与全部确认、重复文件等待选择时取消、关闭窗口触发取消、底层读取延迟返回、GUI 线程不执行大文件 I/O、worker 结束后无存活线程。
7. 性能：构造多文件多工况场景验证源文件扫描次数、流式内存上限和 Header 索引软目标；大文件测试标记为慢速测试，不纳入每次快速单元测试。
8. 端到端：使用脱敏的小型 BLF/CAN FD、真实格式 CSV 和 XLSX 固件验证输出可被 python-can 重新读取，并核对 timestamp、通道、帧类型、标志、报文数、文件名和 Markdown 报告。另用代表性实车 BLF/DBC/CAN ARXML 做发布前人工兼容验证。

### 18.8 实施限制和完成条件

本节描述的是对现有软件的接入约束，不表示 BLF 自动切片已经在当前源码中实现。完成开发后，除第 17 节验收标准外，还必须满足：现有主窗口功能无回归；切片入口不改变当前加载状态；所有大文件操作离开 GUI 线程；取消不强制终止线程；正式基线的运行和打包路径唯一且可复现；依赖清单、打包配置、自动测试和至少一组实际数据验收记录随代码一并交付。

